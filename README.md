# Simple photo frame app in Go

A remotely controlled photo frame. Requires a client that triggers photo change via [protocol](./protocol).

```shell
go install github.com/dennwc/photo-frame@latest
```